# goss bin location
goss_bin_src: https://github.com/aelsabbahy/goss/releases/latest/download/goss-linux-amd64
goss_bin_dest: /usr/local/bin/goss

# goss file location
goss_path: /etc/goss.d

# linux user/group
vault_user: vault
vault_group: vault

# vault settings
vault_data_path: /var/vault/data